@model IEnumerable<AeroportoDatabaseFirst.Models.Aeronave>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QtdPoltronas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoAeronaveNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QtdPoltronas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoAeronaveNavigation.IdTipoAeronave)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAeronave">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAeronave">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAeronave">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
